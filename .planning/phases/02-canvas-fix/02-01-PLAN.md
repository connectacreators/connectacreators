---
phase: 02-canvas-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /var/www/connectacreators/package.json
  - /var/www/connectacreators/package-lock.json
autonomous: false
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05

must_haves:
  truths:
    - "Navigating to /clients/:clientId/followup-builder renders the ReactFlow canvas without crashing"
    - "The NodeToolbar left sidebar is visible with draggable node types (Triggers, Actions, Logic)"
    - "Clicking a node on the canvas opens the NodeConfigPanel in the right panel"
    - "On page load, any previously saved workflow loads from followup_workflows into the canvas"
    - "Clicking Save writes nodes/edges/viewport to followup_workflows for the current client"
  artifacts:
    - path: "/var/www/connectacreators/node_modules/@xyflow/react"
      provides: "ReactFlow canvas library — imported by AIFollowUpBuilder and all node/panel components"
      contains: "package.json"
    - path: "/var/www/connectacreators/package.json"
      provides: "Declared dependency on @xyflow/react"
      contains: "@xyflow/react"
    - path: "/var/www/connectacreators/dist"
      provides: "Production build with ReactFlow included"
      min_lines: 1
  key_links:
    - from: "src/pages/AIFollowUpBuilder.tsx"
      to: "@xyflow/react"
      via: "import { ReactFlow, ReactFlowProvider, ... } from '@xyflow/react'"
      pattern: "from '@xyflow/react'"
    - from: "src/components/followup/nodes/TriggerNode.tsx"
      to: "@xyflow/react"
      via: "import { Handle, Position, NodeProps } from '@xyflow/react'"
      pattern: "from '@xyflow/react'"
    - from: "src/pages/AIFollowUpBuilder.tsx"
      to: "supabase.from('followup_workflows')"
      via: ".upsert() on save, .maybeSingle() on load"
      pattern: "followup_workflows"
---

<objective>
Install the missing @xyflow/react package on the VPS so the AIFollowUpBuilder page stops crashing.

Purpose: The page and all subcomponents are fully written but the app crashes at import time because @xyflow/react is not in node_modules. Installing it and rebuilding is the only change needed.
Output: A production build that includes @xyflow/react, making the canvas render at /clients/:clientId/followup-builder.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Key facts for executor

**Root cause of crash:** `@xyflow/react` is NOT in package.json and not installed. Every file in `src/components/followup/` imports from `@xyflow/react`. The import fails at runtime, producing a blank/crash screen.

**All code is already written — no code changes needed:**
- `src/pages/AIFollowUpBuilder.tsx` — complete, loads/saves workflow, renders canvas
- `src/components/followup/nodes/TriggerNode.tsx` — complete
- `src/components/followup/nodes/ActionNode.tsx` — complete
- `src/components/followup/nodes/ConditionNode.tsx` — complete
- `src/components/followup/panels/NodeConfigPanel.tsx` — complete
- `src/components/followup/panels/NodeToolbar.tsx` — complete
- Route `/clients/:clientId/followup-builder` is already registered in `src/App.tsx`

**VPS deployment:**
- Host: 72.62.200.145, user: root, password: Loqueveoloveo290802#
- Web root: /var/www/connectacreators/
- Build: `cd /var/www/connectacreators && npm run build`
- Reload: `nginx -s reload`

**@xyflow/react version to install:** `@xyflow/react@^12` (current stable as of early 2026; use latest)

**supabase client import path used in AIFollowUpBuilder:**
```typescript
import { supabase } from '../integrations/supabase/client';
```

**followup_workflows table schema (created in Phase 1):**
```sql
client_id UUID NOT NULL UNIQUE,
name TEXT,
nodes JSONB,
edges JSONB,
viewport JSONB,
is_active BOOLEAN DEFAULT false
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @xyflow/react on VPS and rebuild</name>
  <files>/var/www/connectacreators/package.json, /var/www/connectacreators/package-lock.json, /var/www/connectacreators/node_modules/@xyflow/react</files>
  <action>
SSH to VPS (root@72.62.200.145, password: Loqueveoloveo290802#) using an expect script.

Run these commands on the VPS in sequence:

```
cd /var/www/connectacreators
npm install @xyflow/react
npm run build
nginx -s reload
```

`npm install @xyflow/react` will:
1. Download and install the package into node_modules/
2. Add `"@xyflow/react": "^12.x.x"` to package.json dependencies
3. Update package-lock.json

`npm run build` will bundle the app with ReactFlow included. The build MUST succeed with exit code 0 and produce files in dist/.

If the build fails with a TypeScript error about `NodeProps` (the components use `NodeProps` from @xyflow/react with a generic type parameter), that is a known minor TS strict issue — run `npm run build` (not `npm run lint`) which uses `vite build` and is more permissive.
  </action>
  <verify>
    <automated>SSH to VPS and run: ls /var/www/connectacreators/node_modules/@xyflow/react/package.json && grep -q '"@xyflow/react"' /var/www/connectacreators/package.json && ls /var/www/connectacreators/dist/index.html && echo "PASS"</automated>
  </verify>
  <done>
    - @xyflow/react appears in /var/www/connectacreators/node_modules/
    - package.json lists @xyflow/react as a dependency
    - dist/ directory contains a fresh build (dist/index.html exists)
    - nginx reloaded successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify canvas renders correctly in browser</name>
  <files></files>
  <action>Human visual verification of the deployed canvas page.</action>
  <verify>
    <automated>MISSING — human visual verification required for ReactFlow canvas rendering</automated>
  </verify>
  <done>Human confirms the canvas page renders without a crash screen and all three panels are visible.</done>
  <what-built>
    Installed @xyflow/react and rebuilt the app. The AIFollowUpBuilder page at /clients/:clientId/followup-builder should now render the ReactFlow canvas without crashing.
  </what-built>
  <how-to-verify>
    1. Open https://connectacreators.com in the browser (hard refresh: Cmd+Shift+R or Ctrl+Shift+R to bypass cache)
    2. Log in if needed, then navigate to any client detail page
    3. Navigate directly to: https://connectacreators.com/clients/{a-valid-client-id}/followup-builder
       - To find a valid client ID: go to the Clients section and copy the UUID from the URL of any client detail page
    4. Verify the page renders correctly:
       a. LEFT: A sidebar titled "Drag to canvas" with sections for Triggers (FB Lead Trigger), Actions (Send Email, Send SMS, Wait, Update Status), and Logic (Condition) — all items should be visible and draggable
       b. CENTER: The ReactFlow canvas with ~25 pre-wired nodes showing the follow-up workflow (purple trigger node, colored action nodes, orange condition nodes, connecting arrows)
       c. The MiniMap should appear in the bottom-right of the canvas
       d. The Controls toolbar (zoom in/out/fit) should appear on the canvas
    5. Click any node on the canvas — a "Configure Node" panel should slide in on the RIGHT with fields for that node type
    6. Confirm there is NO white screen, NO blank screen, NO "Error" overlay, and NO browser console error like "Cannot find module '@xyflow/react'"
  </how-to-verify>
  <resume-signal>Type "approved" if the canvas renders correctly, or describe what is wrong (e.g., "white screen", "console error: ...", "nodes not showing")</resume-signal>
</task>

</tasks>

<verification>
The phase is complete when:
1. `npm install @xyflow/react` exits 0 on the VPS
2. `npm run build` exits 0 on the VPS and produces dist/
3. Human confirms the canvas renders at /clients/:clientId/followup-builder with toolbar, canvas nodes, and config panel all visible
4. No crash screen or blank screen observed in browser
</verification>

<success_criteria>
- CANVAS-01: Page renders without crashing (ReactFlow canvas visible)
- CANVAS-02: NodeConfigPanel opens when a node is selected
- CANVAS-03: NodeToolbar left sidebar visible with draggable items
- CANVAS-04: Canvas loads saved workflow on page open (DEFAULT_NODES shown on first visit; saved nodes on subsequent visits)
- CANVAS-05: Save button writes to followup_workflows table (upsert by client_id)
</success_criteria>

<output>
After completion, create `.planning/phases/02-canvas-fix/02-01-SUMMARY.md` with:
- What was done (package installed, build rebuilt)
- Build output (time, success/fail)
- Human verification result
- Any issues encountered
</output>
